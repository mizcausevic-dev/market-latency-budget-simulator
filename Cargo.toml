[package]
name = "market-latency-budget-simulator"
version = "0.1.0"
edition = "2024"
description = "Rust and Python simulator for market-data and order-path latency budget risk."
license = "AGPL-3.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
